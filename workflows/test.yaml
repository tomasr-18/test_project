main:
  params:
    - name: company
      type: string
    - name: from_date
      type: string
    - name: to_date
      type: string
    - name: table_name
      type: string

  steps:
    - print_variables:
        call: sys.log
        args:
          message: "company: ${params.company}, from_date: ${params.from_date}, to_date: ${params.to_date}"
